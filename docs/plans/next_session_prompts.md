# Промпти для наступних сесій — TestForTge

Кожен промпт **самодостатній**: копіюйте один блок цілком, нічого не дописуючи.
Спільна «шапка» повторена в кожному навмисно — щоб сесія не мусила її вгадувати.

Усі факти нижче звірені з кодом 2026-08-06, а не взяті з бэклогу.

## Рекомендований порядок

| # | Задача | Обсяг | Чому саме тут |
|---|---|---|---|
| ~~1~~ | ~~E1.6 — claim legacy-проєктів~~ | S | **ЗРОБЛЕНО 2026-08-07** — не переробляти, див. §1 |
| ~~2~~ | ~~E1.5 + E1.9 — таймаути сесій і два тести~~ | S | **ЗРОБЛЕНО 2026-08-07** — не переробляти, див. §2 |
| ~~3~~ | ~~E3.7 — регресія паралельної правки~~ | S | **ЗРОБЛЕНО 2026-08-07** — не переробляти, див. §3. Знайшло реальний дефект |
| ~~4~~ | ~~E0.4 → E1.3 → E1.7 — пошта і що вона розблокує~~ | L | **ЗРОБЛЕНО 2026-08-07** — не переробляти, див. §4. Потрібна дія власника: ключ Resend |
| ~~5~~ | ~~E1.8 — зняти Basic-шлюз~~ | S | **ЗРОБЛЕНО 2026-08-07** — не переробляти, див. §5. R1 закрита повністю |
| 6 | E8/R3 — сховище | XL | **E8.1 затверджено + E8.2 і E8.3 закриті 2026-08-08.** Лишились E8.4–E8.7 |
| ~~7~~ | ~~E4.5a — вкладення до багів~~ | M | **ЗРОБЛЕНО 2026-08-07** на локальному диску свідомо (E8.2 ще заблокований). Знайшло 2 дефекти |
| ~~8~~ | ~~E6.6 / E7.6~~ | M | **ЗРОБЛЕНО 2026-08-07.** E6.6 реалізовано; **E7.6 закрито рішенням замовника — форматів не додаємо** |
| ~~9~~ | ~~E0.11 + E0.12 — keep-alive і квоти~~ | M | **ЗРОБЛЕНО 2026-08-07.** Потрібна дія власника: змінна `KEEPALIVE_URL` в Actions |
| ~~10~~ | ~~E10 — confirmation + регресія~~ | L | **ПРОГНАНО 2026-08-08 — вердикт NO-GO.** Часткове підтвердження видане; повторити після E8 і стейджингу |

---

## ⬛ ШАПКА (входить у кожен промпт нижче)

```
Проєкт TestForTge. Гілка `claude/e9-testing-integration-roles-e2e-83a1e8`,
відгалужена від `claude/testfortge-architecture-planning-9489f6` (уся програма
E0–E9; на `main` її НЕМАЄ). Обидві гілки на origin, CI зелений на останньому
коміті. Цей файл лежить у самій гілці — `docs/plans/next_session_prompts.md`.

ЩО ВЖЕ ЗРОБЛЕНО І ЧОГО НЕ ПЕРЕРОБЛЯТИ:
- E9 закритий увесь. 3901 тест зелений в ОБОХ режимах прапорців.
- E2 (ролі) закритий повністю. E4 (редактори) — окрім E4.5a. E5 (execute) —
  закритий. E3 (сесія → БД) — закритий увесь (E3.7 з 2026-08-07).
- Стан кожної задачі: docs/plans/team_platform_architecture.md, таблиця
  «Покриття вимог — станом на 2026-08-06» у §2. Вона звірена з кодом.

ОБОВ'ЯЗКОВІ ПРАВИЛА (docs/plans/e9_test_strategy.md):
1. Проганяти набір ДВІЧІ: `pytest` і `AUTH_ENABLED=1 ORG_MODE=1 pytest`.
   Другий — режим, у якому продукт їде.
2. Стверджувати звітований результат, а не код відповіді. Усі дефекти цієї
   програми віддавали 200 і рендерили сторінку, що виглядала правильно.
3. Тест, що залежить від режиму, мусить назвати режим.
4. Фікстури в tests/conftest.py НЕ перевигадувати — кожна існує через
   виміряну пастку. `as_user` патчить гейт політики відкритим, інакше
   вигаданий користувач не член нічого і 403 приходить від гейта, а не від
   правила, яке тест перевіряє.
5. Новий POST → або CSRF-захищений, або в EXEMPT у tests/test_csrf_on_every_post.py
   з причиною. Список береться з URL-мапи, тому забути неможливо.
6. Coverage-gate у CI: `--cov-fail-under=93`, гілкове, 19 модулів.
   `pytest-cov` лише в CI, локально `pip install pytest-cov`.

ЧОГО НЕМАЄ НА ЦІЙ МАШИНІ (не витрачайте час):
- `gh` не встановлений, GitHub-конектор не авторизований → PR створити
  НЕМОЖЛИВО. Push працює. Віддавайте готове compare-посилання + опис файлом.
- Немає Postgres і Docker → Postgres-нога тестів іде лише в CI.
- Немає `psql` / `pg_dump`.
- Playwright + Chromium Є, працюють.

РІШЕННЯ ЗАМОВНИКА, ЯКІ НЕ ПЕРЕВІДКРИВАТИ:
- Міграцію на Neon Free ВІДХИЛЕНО 2026-08-06: розгортання демонстраційне,
  місячний скид бази дешевший за міграцію. Не пропонувати знову.
- E0.10 (Batch API) відхилено, обґрунтування в cost_model.md §5.
```

---

## 1. E1.6 — claim legacy-проєктів (двигун є, дії немає) — ✅ ЗРОБЛЕНО 2026-08-07

> **Не виконувати повторно.** Реалізовано: `POST /org/settings/adopt-projects`
> (admin-only, у `POLICY`), картка «Projects with no team» в
> `templates/org_settings.html`, `db.orphan_project_survey()` для превʼю
> кількості та назв, аудит-запис `project/adopt_orphans` з назвами,
> 21 тест у `tests/test_adopt_legacy_projects.py`.
>
> Дві речі, які варто знати, якщо доведеться це чіпати:
> * тести живуть на **власній БД з рівно однією організацією** — на спільній
>   БД набору організацій завжди більше однієї, тож sweep брав би гілку
>   відмови і щаслива дорога ніколи б не виконувалась;
> * перевірку відмови треба робити **на flash, а не на тілі сторінки**:
>   картка налаштувань пише про той самий випадок сама, тому перша версія
>   тесту лишалась зеленою після видалення відмови з роуту. Виміряно
>   мутацією, не вгадано.

<details>
<summary>Оригінальний промпт (для історії)</summary>

```
[ШАПКА]

ЗАДАЧА: E1.6 — міграція legacy-проєктів у акаунт.

СТАН, ПЕРЕВІРЕНО:
- `engine/db.py:2968 adopt_orphan_projects(org_id, only_when_sole_org=True)`
  написана, з захистом «відмовляюсь угадувати, коли організацій більше
  однієї», покрита тестами.
- ЖОДЕН роут її не кличе. Перевірено: `grep -rn adopt_orphan_projects routes/`
  повертає порожньо.
- Наслідок: усі проєкти, створені до ORG_MODE, мають `org_id = NULL`,
  а `db.list_projects(org_id=...)` фільтрує по org_id → вони невидимі
  власним авторам. Це та сама форма дефекту, що «функція без викликача».

ЗРОБИТИ:
1. Роут + UI, яким адміністратор запускає adopt для своєї організації.
   Роут — admin-only, класифікувати в engine/route_policy.POLICY (інакше
   fail-closed гейт його не пустить і тест `test_every_route_declares_a_policy`
   впаде).
2. Показати наперед, СКІЛЬКИ проєктів буде прийнято, і відмову «організацій
   більше однієї» — людською мовою, а не мовчазним нулем.
3. Аудит-запис (`db.append_audit`) — це передача власності.

КРИТЕРІЙ ПРИЙМАННЯ:
- функціональний тест у стилі tests/test_functional_module_matrix.py:
  admin бачить проєкти ПІСЛЯ claim і не бачить ДО; звичайний user отримує
  відмову і кількість проєктів не змінюється;
- відмова при >1 організації перевірена окремим тестом;
- обидва режими прапорців зелені.

НЕ РОБИТИ: не чіпати саму adopt_orphan_projects — її семантика узгоджена.
```

</details>

---

## 2. E1.5 + E1.9 — таймаути сесій і два відсутні тести — ✅ ЗРОБЛЕНО 2026-08-07

> **Не виконувати повторно.** E1.5: `engine/session_timeout.py` — два
> годинники, `classify()` чиста функція від `(stamps, now)`, тому жоден тест
> не спить. Прострочена сесія читається як анонімна ще до гейта політики,
> серверний рядок видаляється, fetch отримує `session_expired`. Вікна в
> env: `SESSION_IDLE_MINUTES=240`, `SESSION_ABSOLUTE_HOURS=24`, обидва в
> render.yaml. E1.9: `tests/test_auth_security_vectors.py`.
>
> **Одне уточнення до цього промпту, перевірене в коді.** Він казав, що
> timing і enumeration «без названого тесту». Насправді названі тести були
> — `TestAccountEnumeration` і
> `test_an_unknown_address_returns_the_identical_body`. Проблема була
> інша й гірша: вони не перевіряли того, що обіцяли. Один дивився на
> константу замість того, щоб виконати обидві гілки; другий, попри назву,
> порівнював підрядок, а не тіла сторінок. Зелений тест із правильною
> назвою — дорожчий за відсутній, бо його не видно в жодному списку
> прогалин.
>
> Три речі, які варто знати, якщо доведеться це чіпати:
> * **сесія БЕЗ штампів не викидається.** Кожна жива сесія на момент
>   деплою така; читати «немає штампа» як «нескінченно стара» — значить
>   вигнати всіх користувачів деплоєм самої фічі;
> * **хук реєструється в `app.py`, а не в `routes/__init__.py`**, і саме
>   через порядок: він мусить бігти ДО хука холодного старту, інакше
>   `_session_active_since` вже дописаний і «сесія прийшла порожня» більше
>   не відрізнити — а це єдиний спосіб сказати «ми втратили вашу сесію»
>   замість «ви були неактивні». На free-плані перше — звичайний випадок;
> * **порівняння сторінок на enumeration тікало двічі.** Спершу regex
>   пропустив `<meta name="csrf-token">` (6 падінь із 40); потім
>   заморозка на `jinja_env.globals` не подіяла взагалі, бо `csrf_token`
>   приходить у шаблон ТРЬОМА реєстраціями і context processor перебиває
>   jinja-global (ще 2 із 60). Обидва рази вимірено циклом, не вгадано.
>   Тепер є детермінований тест, що заморозка справді дійшла до сторінки.
>
> Залишковий риск, який НЕ закривався і є рішенням оператора: **lockout —
> це enumeration-оракул** ціною п'яти запитів на адресу. Обґрунтування й
> ціна обох варіантів фіксу — у docstring
> `test_a_locked_account_is_the_one_deliberate_exception`.

<details>
<summary>Оригінальний промпт (для історії)</summary>

```
[ШАПКА]

ЗАДАЧА: E1.5 (залишок) + E1.9 (залишок).

СТАН, ПЕРЕВІРЕНО:
E1.5 — зроблено: ротація session id у `permissions.login_user`,
`logout_user(everywhere=True)`, тест на fixation у
tests/test_access_control_branches.py:114.
E1.5 — НЕ зроблено: idle + absolute timeout. У коді немає нічого.
`_session_active_since` в app.py:619 — це маркер холодного старту
(`SERVER_START_TIME`), НЕ годинник сесії. Не переплутайте.

E1.9 — покрито: CSRF (tests/test_csrf_on_every_post.py, список з URL-мапи,
fail-closed), session fixation, open redirect `next=`
(test_auth_password.py::test_next_cannot_redirect_off_site),
brute-force через lockout (test_the_lockout_message_is_safe_to_show).
E1.9 — БЕЗ названого тесту: timing і user enumeration.

ЗРОБИТИ:
1. Idle-таймаут і абсолютний таймаут сесії. Врахувати: на free-плані Render
   сервіс засинає і SESSION_TYPE=filesystem витирається — тобто «сесія
   зникла» вже трапляється і без таймауту; таймаут має бути відрізняти цей
   випадок від протермінованого.
2. Тест на timing: відповідь на неіснуючий email і на існуючий з невірним
   паролем не повинна відрізнятися за часом настільки, щоб перебирати.
3. Тест на enumeration: повідомлення й код відповіді однакові для
   «немає такого користувача» і «невірний пароль».

КРИТЕРІЙ: кожен із шести векторів E1.9 має названий негативний тест;
таймаути мають тест, що не спирається на sleep реального часу.

ПАСТКА: тест на timing легко робиться flaky. Політика — нуль flaky, без
карантину. Якщо стабільно не виходить — вимірюйте не час, а те, що обидві
гілки виконують однакову роботу (наприклад, що хеш перевіряється завжди).
```

</details>

---

## 3. E3.7 — регресія паралельної правки — ✅ ЗРОБЛЕНО 2026-08-07

> **Не виконувати повторно.** `tests/test_parallel_edits.py` (двоє РІЗНИХ
> людей, рядок + пак + справжня гонка) і один браузерний тест у
> `test_e2e_golden_paths.py::TestTwoPeopleInOneTeam`.
>
> **Уточнення до промпту.** Він казав, що оптимістичне блокування
> «перевірене лише в юніт-тестах». Насправді 409 був покритий і по HTTP:
> пакетний рівень у `test_write_conflicts.py::TestConflictOverHttp` (зі
> справжнім переплетеним записом), рядковий — у
> `test_editors_contract.py::test_a_stale_version_is_a_409` для КОЖНОЇ
> сутності. Справжня прогалина була вужча й цікавіша: усі наявні тести
> конфліктів використовують ОДНОГО клієнта зі стейлим номером версії, тож
> ніщо не перевіряло властивостей, які існують лише коли пишуть двоє різних
> людей — і ніщо не перевіряло рядковий замок під справжньою
> паралельністю.
>
> **І саме там був дефект.** `editable.patch()` робив read-then-compare
> (SELECT → порівняння в Python → запис без предиката версії) замість
> одного умовного UPDATE, як це правильно зроблено в
> `db._claim_pack_write`. Вісім потоків на один рядок → **п'ять
> переможців**. Виправлено: `editable._claim_row()` + умовний DELETE у
> `remove()`.
>
> Головний урок, якщо доведеться писати схожий тест: **вісімпотокова версія
> ловила цей дефект 0/10 на незавантаженій машині** і падала лише під повним
> набором, де БД зайнятіша й вікно ширше. Детермінований тест, який форсує
> переплетення (запис колеги — всередині нашого читання, з іншого потоку),
> ловить 10/10. Обидва в файлі; гарантія — на детермінованому.

<details>
<summary>Оригінальний промпт (для історії)</summary>

```
[ШАПКА]

ЗАДАЧА: E3.7 — двоє в одному проєкті, паралельні правки.

СТАН, ПЕРЕВІРЕНО:
- Спільний проєкт двома РЕАЛЬНИМИ сесіями браузера вже покрито:
  tests/test_e2e_golden_paths.py::TestTwoPeopleInOneTeam. Зелене в CI,
  анти-flaky gate ×10.
- НЕ покрито: одночасна правка того самого рядка. Оптимістичне блокування
  (`row_version`, 409, WriteConflict) перевірене лише в юніт-тестах.

ЗРОБИТИ: сценарій, де двоє відкривають один тест-кейс, обидва редагують,
другий отримує 409 і людське «перезавантажте», а НЕ тихе затирання.
Плюс те саме на рівні пака (tc_version / cl_version) — wipe-and-replace
save зі стейлою версією.

КРИТЕРІЙ: тест падає, якщо прибрати перевірку версії. Перевірте це
буквально — тимчасово приберіть і переконайтесь, що червоніє.

ДЕ ПИСАТИ: поруч із tests/test_write_conflicts.py (юніт-рівень уже там)
або в test_e2e_golden_paths.py, якщо ведете через браузер.
```

</details>

---

## 4. E0.4 → E1.3 → E1.7 — пошта і залишок R1 — ✅ ЗРОБЛЕНО 2026-08-07

> **Не виконувати повторно.** `engine/mailer.py` (Resend одним POST, без
> SDK), таблиця `auth_token` + `create/get/consume/claim_send/revoke`,
> сторінки `auth_forgot` / `auth_reset` / `auth_verify`, банер
> непідтвердженої адреси в `base.html`, рядки в en+ua,
> `RESEND_API_KEY`/`MAIL_FROM`/`MAIL_DAILY_LIMIT` у render.yaml.
> 52 тести в `tests/test_email_and_tokens.py`.
>
> **ДІЯ ВЛАСНИКА, без якої це не працює на проді:** завести домен у Resend
> і вписати `RESEND_API_KEY` + `MAIL_FROM` у дашборд Render. Без них усе
> лишається робочим — інвайти віддають лінк, як і до цього епіка, — але
> скидання пароля недосяжне, бо лінку нема куди йти.
>
> **Головна знахідка, і вона змінює постановку.** Промпт просив
> «верифікацію email». Перевірка коду показала, що тригера для неї не
> існувало: обидва шляхи створення акаунта ставили `email_verified=True`,
> тож верифікація була б п'ятою «функцією без викликача» в цій програмі.
> Але сам той `True` був **припущенням, записаним як факт** — воно
> справджується лише коли інвайт справді надіслали поштою, а поки
> провайдера не було, лінк щоразу передавали руками. Тому E0.4 додає
> `Invite.emailed_at`, прапорець іде за доставкою, і саме це дає верифікації
> справжній тригер: акаунт із переданим руками інвайтом бачить банер
> «адресу не підтверджено».
>
> Ще три речі, які варто знати:
> * **`/auth/forgot` — це enumeration-поверхня.** Однакової сторінки мало:
>   гілка, що знайшла акаунт, створює токен і робить HTTPS-виклик, а гілка,
>   що не знайшла, повертається миттєво. Різниця вимірна ззовні. Тому
>   відправка йде в потік (`_dispatch`), і час відповіді не залежить від
>   того, що знайшли. Тест порівнює сторінки цілком — із замороженим CSRF,
>   інакше він блимає (див. §2).
> * **Токен споживається на POST, не на GET.** Деякі поштові клієнти
>   витягують кожен URL із листа для прев'ю; consume на GET спалював би
>   лінк до того, як людина його побачить. І хибне підтвердження пароля
>   теж не витрачає лінк — інакше друкарська помилка відправляє людину по
>   новий лист, а це ще одне з сотні повідомлень на добу.
> * **Пастку з промпту («не слати підтвердження туди, де адреса вже
>   доведена») перевіряє окремий тест** — акаунт із надісланого поштою
>   інвайту не отримує жодного листа підтвердження.

<details>
<summary>Оригінальний промпт (для історії)</summary>

```
[ШАПКА]

ЗАДАЧА: E0.4 (email-провайдер) і те, що він розблоковує — E1.3, залишок E1.7.

СТАН, ПЕРЕВІРЕНО:
- Провайдера пошти НЕМАЄ взагалі. Нічого не шле.
- Інвайти працюють так: адмін створює лінк, і роут ФЛЕШИТЬ його адміну,
  щоб той переслав руками (routes/members.py, коментар це прямо пояснює:
  сказати «лист надіслано» було б брехнею).
- Немає верифікації email і скидання пароля: одноразових токенів з TTL у
  коді немає (є лише Invite з TTL 168 год — це інший механізм).
- UI: templates/auth_login.html і auth_invite.html Є. Сторінок reset /
  verify немає.

ЗРОБИТИ:
1. E0.4: провайдер (cost_model.md називає Resend у безкоштовному наборі)
   + fallback «скопіювати лінк», який лишається робочим, коли ключа немає.
   Ключ провайдера — секрет, `sync: false`, не в git.
2. E1.3: одноразові токени з TTL для verify і reset. Взяти за зразок
   `db.create_invite / consume_invite` — там уже правильна форма:
   claim одним умовним UPDATE, а не читання-потім-запис (це виправляли
   в E9.3, бо інакше двоє претендентів обидва проходять).
3. E1.7: сторінки reset / verify, i18n EN+UA, CSP-сумісно (nonce,
   без inline-обробників).

КРИТЕРІЙ: лист не надсилається двічі на один токен; протермінований токен
відмовляє однаково для «немає» / «використаний» / «протух» (не підказувати,
які токени існували); usable без провайдера (fallback).

ПАСТКА: на безкоштовному плані ліміт ~100 листів/добу — не слати
підтвердження там, де адресу вже доведено (інвайт уже прийшов на пошту).
```

</details>

---

## 5. E1.8 — зняти HTTP Basic-шлюз — ✅ ЗРОБЛЕНО 2026-08-07

> **Не виконувати повторно.** Прапорець `BASIC_GATE_ENABLED` (дефолт **1**),
> рішення на кожен запит, інтерлок проти пари `0` + `AUTH_ENABLED=0`,
> винятки з `route_policy.MACHINE`. 40 тестів у `tests/test_basic_gate.py` —
> модуль до цього не мав жодного.
>
> **Уточнення до промпту.** Він казав перевірити, що машинні ендпоінти «не
> залежать» від шлюзу, «бо вони вже в OPEN у engine/route_policy». Це
> сплутує два різні гейти: `OPEN` керує політикою СЕСІЙ, а в Basic був
> власний перелік шляхів. Виміряно: з піднятим шлюзом усі п'ять машинних
> ендпоінтів (`/api/recorder-session/*`, `/api/browser/*`,
> `/automation/allure-results`) віддавали 401 з тілом «Authentication
> required.» — тобто **на проді розширення і CI не працювали**, і ніхто
> цього не бачив, бо 401 від гейта не відрізнити від 401 від ендпоінта.
>
> Три речі, які варто знати:
> * **`BASIC_GATE_ENABLED` — єдиний прапорець у реєстрі з дефолтом on.**
>   Правило «усе за замовчуванням вимкнено» стосується НОВОЇ поведінки;
>   прапорець, що знімає живий периметр, при дефолті off зняв би його на
>   наступному ж деплої. `tests/test_features.py::DEFAULTS_ON` тримає
>   виняток названим, щоб другий такий не з'явився випадково.
> * **Хук ставиться завжди, коли є пароль; рішення — на кожен запит.** З
>   реєстрацією хука за прапорцем при імпорті перемикач нічого б не робив
>   до рестарту, а периметр, який не перевірити без деплою, ніхто не
>   перевіряє.
> * **Пастка тестування:** `tests/test_basic_gate.py` мусить вимикати
>   `TESTING`, бо гейт сам себе обходить під ним. І НЕ перезавантажуйте
>   модуль `app` у фікстурі — перша версія робила `importlib.reload(app)`,
>   набір лишався зеленим, а **coverage-прогін (той, на якому стоїть CI)
>   падав 17 тестами у трьох сторонніх файлах**. Виміряно проти stash-базлайну:
>   0 падінь до, 17 після. Правильно — вставити хук у
>   `app.before_request_funcs[None]` на позицію 0 і прибрати в teardown.

<details>
<summary>Оригінальний промпт (для історії)</summary>

```
[ШАПКА]

ЗАДАЧА: E1.8 — прибрати Basic-шлюз під прапорцем.

СТАН, ПЕРЕВІРЕНО:
- Шлюз активний на проді (у логах старту: «HTTP Basic Auth gate is active»).
- Вмикається змінними TESTFORTGE_BASIC_USER / TESTFORTGE_BASIC_PASSWORD,
  оголошеними в render.yaml:217,219. Реалізація — engine/basic_auth.py,
  має TESTFORTGE_BASIC_PUBLIC_PATHS для винятків.

РОБИТИ ТІЛЬКИ ПІСЛЯ: E1.3 + E1.7 (інакше зняття шлюзу відкриє інстанс,
у якого ще немає ані верифікації пошти, ані скидання пароля).

ЗРОБИТИ: прапорець, який вимикає Basic після виходу E1; перевірити, що
/healthz, /readyz і token-автентифіковані машинні ендпоінти не залежать
від нього (вони вже в OPEN у engine/route_policy).

КРИТЕРІЙ: з вимкненим Basic анонім бачить тільки сторінку входу, а
машинні виклики працюють по своїх токенах.
```

</details>

---

## 6. E8 / R3 — сховище — ✅ E8.1 ЗАТВЕРДЖЕНО + E8.2 і E8.3 ЗРОБЛЕНІ 2026-08-08

> **E8.1 зроблено 2026-08-07 і зупинено на рев'ю, як просив промпт**
> ([adr/0002-artifact-storage.md](adr/0002-artifact-storage.md)).
> **Замовник затвердив ADR 2026-08-08, включно з §4.1**, і E8.2 виконано
> того ж дня.
>
> **E8.2 — що саме поставлено.** `engine/storage.py`: інтерфейс `Backend` на
> чотири дієслова (`put` / `get_bytes` / `exists` / `locate` /
> `delete_prefix`), `LocalBackend` і `S3Backend`. `engine/blobs.py` більше
> не пише на диск сам — він лишається власником *політики* (які типи файлів
> взагалі можна прикріпити, що бачить людина при відмові, як виглядає
> ключ), а байти віддає бекенду. `routes/bugs.py` і `routes/chat.py`
> змінились рівно на передачу організації, як і обіцяв коментар в E4.5a.
>
> **Критерій приймання перевірений буквально, а не переказаний.** «Зміна
> бекенда — конфіг, не деплой нового коду» — це твердження про *запущений
> процес*, тож `TestTheAcceptanceCriterion` міняє змінну середовища ПІСЛЯ
> імпорту й питає ще раз. Тому `backend_for()` читає конфіг на кожному
> виклику й нічого не кешує на рівні модуля. Перевірено мутацією: обгортка
> `functools.lru_cache` навколо `backend_for` валить 16 тестів; читання
> `STORAGE_BACKEND` на імпорті — 5.
>
> **Один пункт ADR закрито при виконанні, і не на користь ADR.** §4.5
> пропонував `boto3`. Вимір: `boto3` + `botocore` — 15.2 МБ wheel, `minio` —
> 91 КБ (≈1.9 МБ нового в проєкт). Різниця у вісім разів на вільному дино,
> де розмір образу — це час деплою й холодного старту. ADR тепер містить
> таблицю виміру й рядок «пропонували A, зміряли, взяли B», бо ADR, у якому
> завжди правий перший варіант, ніхто не читає.
>
> **E0.5 (бакет + креденшели) НЕ була блокером** — усупереч тому, що
> залежність у таблиці E8.2 казала. `local` лишається дефолтом, і бакет
> потрібен лише щоб `s3` увімкнути; вимагати його до написання абстракції
> означало б чекати на дію власника заради коду, який без неї працює.
>
> **E8.3 — Admin UI, і чому найбільший клас тестів про повідомлення.**
> Критерій приймання — «невірні креденшели → зрозуміла помилка на кроці
> перевірки» — це твердження про *текст*, тож `S3Backend._diagnose` розводить
> п'ять причин на п'ять різних наступних дій: перепаяти ключ, виправити назву
> бакета, додати `s3:PutObject` до політики, полагодити годинник сервера,
> перевірити адресу й порт. Панель, яка на всі п'ять каже «storage error»,
> формально критерій задовольняє й нікому не допомагає.
>
> Два рішення, які варто знати:
> * **перевірка пише, читає й видаляє**, а не питає «чи існує бакет». Ключ
>   лише з `s3:ListBucket` проходить будь-яку перевірку існування, а потім
>   падає на першому аплоаді — через годину, в іншому запиті, в іншої людини.
>   Видалення теж перевіряється, бо E8.5 на нього спирається;
> * **Save спершу перевіряє й відмовляється зберігати те, що не працює.**
>   `backend_for` при зламаному конфізі відкочується на локальний диск, а він
>   ефемерний — тож збережений зламаний конфіг означає зелене «Збережено» над
>   налаштуваннями, які тихо шлють докази команди на диск, що зникне при
>   наступному рестарті.
>
> **Четвертий дефект знайшов власний тест E8.3, і він серйозніший за три
> нижче.** `LocalBackend.delete_prefix` перевіряв `isdir`, тож префікс, який
> вказує на **один об'єкт**, не збігався ні з чим: повертав 0 і мовчки нічого
> не видаляв. На S3 той самий виклик видаляє цей об'єкт, бо ключ є префіксом
> самого себе. Симптом у E8.3: кожне натискання «Test connection» на дефолтному
> бекенді лишало файл у `STORAGE_ROOT` **і рапортувало успіх**. Але важить
> інше: **E8.5 «видалити дані проєкту» ходить через цей самий метод**, і
> розбіжність між бекендами там означала б видалення, яке не видаляє. Знайдено
> тільки тому, що тест перевіряв диск ПІСЛЯ перевірки, а не текст відповіді.
>
> **Три дефекти знайшов смоук-прогін панелі, а не рев'ю коду:**
> 1. невалідна назва бакета (`minio` валідує її локально, без коду S3)
>    діагностувалась як «не вдалось достукатись до endpoint» — тобто адміна
>    відправляли перевіряти DNS через одруківку трьома полями вище;
> 2. `https://…` у полі endpoint із **знятим** чекбоксом HTTPS давав
>    з'єднання відкритим текстом на порт 80. Браузер не надсилає знятий
>    чекбокс узагалі, тож «знято» і «поля не було» — це та сама відправка;
>    явна схема тепер сильніша за чекбокс;
> 3. перевірка недосяжного хоста тривала ~6 с через дефолтні ретраї клієнта —
>    при фаєрволі це переживає таймаут самого дино, і адмін отримує порожню
>    сторінку замість діагнозу. Тепер обмежена (5 с, один ретрай) — **тільки
>    для перевірки**, аплоади лишаються з бібліотечними ретраями.
>
> **Що лишилось:** E8.4 (бекапи) → E8.5 (retention і видалення даних
> проєкту) → E8.6 (self-host) → E8.7 (тести проти реального бакета).
> Прапорець `STORAGE_BACKEND_CONFIGURABLE` лишається `0` до E8.7 — за ним
> тепер є і код, і UI, але немає прогону проти живого бакета, а це гейт 3
> самого ADR.
>
> **Історична довідка — що потрібно було від замовника (закрито):**
> §4.1 ADR звужує список бекендів до `local` + `s3`-сумісного, а §5.1 плану
> називає ще Azure Blob. S3-сумісний адаптер покриває R2, AWS S3, B2,
> Wasabi і MinIO одним протоколом; Azure — це інший API, тобто окремий
> адаптер, окремі креденшели й окремі тести на відмови, яких ніхто не може
> прогнати без акаунта. Якщо Azure потрібен комусь конкретному вже зараз —
> це треба знати ДО E8.2, бо змінюється не код, а порядок робіт.
>
> Два виміряні факти з ADR, які варто знати незалежно від рішення:
> * **аплоади сьогодні затирають одне одного** — `routes/estimation.py:391`
>   зберігає під `secure_filename()` без скоупу проєкту, тож
>   `requirements.docx` двох проєктів це один файл. Схема ключа §4.2 робить
>   це неможливим за побудовою, а не «виправляє»;
> * **`AUTOMATION_RUN_RETENTION_DAYS=1` / `MAX_KEPT=5` — це не політика, а
>   симптом.** Коментар у `engine/automation_runner.py:29` каже прямо: диск
>   ефемерний, попередній дефолт у 14 днів давав 502 через OOM. Артефакти
>   викидають, бо їх нема куди покласти.
>
> Приємний наслідок ефемерності: **мігрувати наявні файли не треба** —
> нічого довговічного на диску немає.

<details>
<summary>Оригінальний промпт (для історії)</summary>

```
[ШАПКА]

ЗАДАЧА: епік E8 — вимога R3 «сховище для проєктів». Сім задач E8.1–E8.7,
жодної не почато. Це ЄДИНА вимога замовника з нульовим покриттям.

СТАН, ПЕРЕВІРЕНО: у engine/ немає абстракції сховища. Прапорець
STORAGE_BACKEND_CONFIGURABLE оголошений у engine/features.py і присутній у
render.yaml, але за ним нічого немає.

ВАЖЛИВЕ УТОЧНЕННЯ ЗАМОВНИКА (plan §5.1, не переінтерпретовувати):
R3 = pluggable **blob**-сховище + ціль бекапів + self-host.
Це НЕ pluggable СУБД. Postgres лишається Postgres.

ПОЧАТИ З E8.1 — ADR «обране сховище», і зупинитись на ньому для рев'ю.
Не писати код до ухвалення ADR. Зразок форми: docs/plans/adr/0001-*.md.

ADR має відповісти:
- local disk / S3-сумісне / Azure Blob — які саме, і чому не всі одразу;
- конфіг на організацію чи на інстанс;
- що робиться зі скріншотами й відео прогонів (сьогодні вони НЕ в БД —
  cost_model.md прямо забороняє класти їх у Postgres);
- як виглядає відмова сховища: fallback чи явна помилка.

ДАЛІ (окремими сесіями, не все за раз): E8.2 абстракція → E8.3 Admin UI →
E8.4 бекапи/експорт-бандл → E8.5 retention і видалення даних проєкту →
E8.6 self-host + hardening docker-compose → E8.7 тести, включно з відмовами.
```

</details>

---

## 7. E4.5a — вкладення до багів — ✅ ЗРОБЛЕНО 2026-08-07

> **Не виконувати повторно.** `POST /bug-reports/<id>/attach`,
> `engine/blobs.py`, форма в картці бага, 33 тести в
> `tests/test_bug_attachments.py`. Сховище — **локальний диск свідомо**:
> промпт давав вибір (чекати E8.2 або обмежитись диском і написати це в
> тесті), і обрано друге, бо ADR 0002 ще не затверджений. Обмеження
> зафіксоване в `TestTheLocalDiskLimitation`, включно з тестом «диск
> зник — рядок далі показує вкладення, а воно 404».
>
> **Однина/множина розведені свідомо** (промпт просив саме це):
> `bug_report.attachment` — одна колонка з текстовим *посиланням* із
> баг-таблиці команди, ходить через імпорт/експорт; `attachments` у `extra`
> — список ключів сховища для галереї. Аплоад пише другий і не чіпає
> перший. Злити їх означало б або зламати round-trip таблиці, або
> сплющити галерею в один слот.
>
> **Два дефекти, знайдені по дорозі:**
> * **чат уже вмів прикріпляти файл — і той файл не віддавався.**
>   `routes/chat.py` писав у `UPLOAD_FOLDER/chat_bug_attachments/`, а
>   сторінка віддає через `automation_asset` із `STORAGE_ROOT`. Виміряно
>   ДО правки: аплоад 200, рядок записаний, галерея 404;
> * **форма всередині форми.** Картки багів лежать в одному
>   `<form action="/bugs/bulk">`, тож вкладений `<form>` браузер викидає, і
>   кнопка відправляла б файл у `/bugs/bulk`. Серверний тест
>   (`"/attach" in body`) цього НЕ бачить — побачив браузер. Полагоджено
>   атрибутом `form=`; структурна перевірка тепер у
>   `TestTheUploadFormIsRealHtml`.
>
> Пастка для того, хто писатиме схожу перевірку: рахуючи теги `<form>` у
> HTML, треба спершу вирізати коментарі **і тіла `<script>`** — у
> `_project_picker.html` рядок `<form action>` зустрічається в обох, і
> обидва рази дає фантомну знахідку.

<details>
<summary>Оригінальний промпт (для історії)</summary>

```
[ШАПКА]

ЗАДАЧА: E4.5a — аплоад скріншота/відео до бага руками. Залишок R7.

СТАН, ПЕРЕВІРЕНО:
- `bug_report.attachment` — це ОДНА колонка VARCHAR(500) (engine/db.py:1031),
  тобто шлях/URL, а не файл і не список.
- Шаблон templates/bug_reports.html:340 рендерить `bug.attachments`
  (МНОЖИНА) — вони приходять через JSON-колонку `extra`, не через
  `attachment`. Ця розбіжність однини й множини існує; розберіться з нею
  свідомо, а не випадково.
- Готовий зразок аплоаду вже є: routes/estimation.py:391
  `_save_uploaded_attachment()` і `_save_uploaded_mockups()`.

ЗАЛЕЖНІСТЬ: куди класти файл — це E8/E0.5. Без цілі зберігання вийде
запис на ефемерний диск дино, який зникає при рестарті. Або робіть після
E8.2, або свідомо обмежтесь локальним диском і напишіть це в тесті.

КРИТЕРІЙ: файл, доданий до бага, видно на сторінці бага після перезаходу
іншою сесією (а не лише в тій, що вантажила).
```

</details>

---

## 8. E6.6 і E7.6 — контекст проєкту в Tedgie, формати експорту — ✅ ЗРОБЛЕНО 2026-08-07

> **E7.6 — закрито рішенням замовника, коду немає.** Промпт вимагав спершу
> спитати, які саме формати потрібні. Запитано з варіантами (XLSX / JSON /
> PDF / нічого) — обрано **нічого**: CSV уже слідує за фільтром і
> відкривається в Excel як є, тож XLSX додав би форматування, а не дані.
> Переглянути, коли з'явиться названий споживач.
>
> **E6.6 — реалізовано.** `engine/project_facts.py`, `respond(...,
> project_id=)`, 50 тестів у `tests/test_project_facts.py`. Golden set
> лишився 100%/100%.
>
> Три речі, які варто знати:
> * **це окремий шар, а не пак.** `mentoring` навмисно чистий — YAML на
>   вході, текст на виході, жодної БД, імпортується без Flask. Кількість
>   багів — не домашнє правило, і новий пак із тригерами «bugs», «test
>   cases» крав би питання в `severity_priority`;
> * **умов три, і всі три потрібні:** кількісний маркер + перша особа +
>   названий артефакт, плюс **вето на модальність**. Виміряно на самому
>   golden set: питання «How many severity levels should we have?» проходить
>   перші дві умови і рятується лише тим, що не називає артефакт. Варіант
>   «how many bugs should we have open at once» не врятувався б — звідси
>   вето на `should` / `варто`;
> * **пастка E6 дотримана:** тести перевіряють, що у відповіді Є ПОРАДА
>   (структура, порядок дій, названа найгірша серйозність), і НЕ перевіряють
>   жодного конкретного слова.
>
> **Побічна знахідка, важливіша за саму задачу:** `tedgie_eval.run_rule_layer`
> обіцяв у docstring проганяти шари «в порядку, який використовує
> `respond()`», а кликав два шари напряму — тобто гейт 100%/100%
> структурно не бачив би нового шару всередині `respond()`. Виправлено;
> тепер гармата міряє той ланцюг, який їде.

<details>
<summary>Оригінальний промпт (для історії)</summary>

```
[ШАПКА]

ЗАДАЧА: два незалежні залишки. Можна робити разом або окремо.

E6.6 — СТАН, ПЕРЕВІРЕНО:
`engine/mentoring.py` має `route()` (рядок 137) і
`answer(message, lang="en", *, packs=None)` (рядок 286).
У `answer()` НЕМАЄ параметра project_id — тобто Tedgie фізично не може
побачити артефакти активного проєкту. Це і є незакрита частина.
ЗРОБИТИ: передати контекст активного проєкту в retrieval, щоб на «скільки
в мене відкритих багів» відповідало число, а не загальна порада.
ПАСТКА (E6, зафіксована): гарантувати треба ПОРАДУ, а не термін — не
писати тест, який вимагає конкретного слова у відповіді моделі.
ГЕЙТ: golden set у CI на 100%/100% (engine/qa_knowledge/eval/golden_set.yaml)
— він мусить лишитись зеленим.

E7.6 — СТАН, ПЕРЕВІРЕНО:
є лише `dashboard_export_csv` (routes/dashboard.py:193), і він коректно
слідує за поточним фільтром.
ЗРОБИТИ: інші формати експорту, якщо вони потрібні. Спершу спитайте
замовника, які саме — у плані це не конкретизовано, і робити XLSX «бо
можна» буде витратою.
```

</details>

---

## 9. E0.11 + E0.12 — keep-alive і квоти — ✅ ЗРОБЛЕНО 2026-08-07

> **Не виконувати повторно.** `.github/workflows/keepalive.yml`,
> `engine/capacity.py`, картка «Capacity» в налаштуваннях, 37 тестів у
> `tests/test_capacity.py`.
>
> **ДІЯ ВЛАСНИКА:** завести змінну `KEEPALIVE_URL` (Settings → Secrets and
> variables → Actions → **Variables**, не Secrets) зі значенням
> `https://<app>.onrender.com/healthz`. Без неї workflow свідомо no-op.
>
> **Головна цифра, задля якої усе це так, а не інакше.** Промпт попереджав,
> що keep-alive на free-плані має ціну — вона така: тариф дає **750
> інстанс-годин на місяць**, а цілодобовий пінг тримає сервіс живим ~730
> годин, тобто з'їдає 97% ліміту й зупиняє сервіс до кінця місяця.
> Цілодобовий keep-alive **вимикає** сервіс. Тому вікно Пн–Пт 06:00–17:59
> UTC ≈ 264 год/міс, і тест `test_the_window_fits_inside_the_free_allowance`
> перевіряє саме цю арифметику, а не наявність файлу.
>
> **Про «чесний екран холодного старту» — уточнення до промпту.** Його не
> можна зробити в тому вигляді, як він звучить: перший запит чекає тому, що
> процес, який мав би намалювати екран «сервіс прокидається», — це і є
> процес, який стартує. Нічого не запущено, отже нічого не рендериться.
> Тому замість вигаданого екрана — **виміряна** величина: один audit-рядок
> на кожен запуск процесу, і сторінка каже «сервіс засинав і стартував N
> разів за добу». Це та цифра, за якою видно, чи варто вмикати keep-alive.
>
> **Квоти.** Рядки на організацію — точні; байти — **оцінка** з виміряних
> розмірів рядка (cost_model §3), і так і підписано: Postgres не вміє
> сказати частку однієї організації у спільній таблиці. Записи **не
> блокуються** — на демо-розгортанні повна база не має ставати зламаним
> продуктом, а рішення «лишаємось на Render free» від 2026-08-06 не
> перевідкривається; є тест, що картка не каже «upgrade» чи «migrate».

<details>
<summary>Оригінальний промпт (для історії)</summary>

```
[ШАПКА]

ЗАДАЧА: E0.11 (keep-alive + чесний UI холодного старту) і E0.12 (квоти).

СТАН, ПЕРЕВІРЕНО: ні того, ні того в коді немає. У render.yaml немає cron.

КОНТЕКСТ, ВИМІРЯНИЙ 2026-08-06: `curl /readyz` на проді віддав 200 за
45 секунд — це холодний старт Render free. Тобто проблема реальна, а не
теоретична: перший відвідувач чекає майже хвилину без пояснення.

E0.11: keep-alive (cron/зовнішній пінг) + чесний екран «сервіс
прокидається», а не порожня сторінка. Врахуйте, що keep-alive на
безкоштовному плані має свою ціну — не тримайте дино живим цілодобово,
якщо це з'їдає місячний ліміт годин.

E0.12: квоти на організацію під ліміти 0.5 ГБ БД. Розмір БД читається
запитом з docs/runbooks/database-on-a-free-plan.md §3.1.

ВАЖЛИВО: база лишається на Render free за рішенням замовника — місячне
протермінування приймається як ризик. Квоти НЕ мають вигляду «мігруймо».
```

</details>

---

## 10. E10 — confirmation + повна регресія — 🟡 ПРОГНАНО 2026-08-08, вердикт NO-GO

> **Звіт: [e10_confirmation.md](e10_confirmation.md).** Прогнано попри
> невиконані вхідні критерії — свідомо: вихід фази 1 це трасована матриця з
> вердиктами, а «вимога не реалізована» теж вердикт. Відмовитись від усієї
> вправи через одну відсутню вимогу означало б сплутати гейт зі звітом.
>
> **Вердикт: NO-GO для підписання. Часткове підтвердження видане** — 9 із
> 10 вимог мають трасовані кейси й PASS; R3 не реалізована.
>
> **Що заміряно:** 4 240 тестів зелені в обох режимах прапорців · браузерні
> golden paths 10/10 · coverage 93.39% · golden set Tedgie 100%/100% ·
> деградації в периметрі немає.
>
> **Що розблокує GO (у порядку):** рішення по ADR 0002 §4.1 → E0.5 (бакет) →
> E8.2–E8.7 → стейджинг → повторний прогін матриці.
>
> Дві речі, які варто знати, якщо повторюватимете:
> * **набір не можна ганяти паралельно з собою.** Фонова регресія і
>   браузерний гейт, запущені одночасно, дали 9 фальшивих падінь; та сама
>   нога наодинці — 4 240 зелених. Причина: `conftest.py` фіксує один шлях
>   скретч-бази, а застосунок пише файлові сесії в один каталог. Занесено як
>   M-1 у звіті;
> * **посилання у звіті треба звіряти з набором.** Перша редакція матриці
>   називала два неіснуючі тестові файли і один неіснуючий модуль. Звіт
>   підтвердження, що посилається на неіснуючий тест, — це та сама помилка,
>   заради виявлення якої він пишеться, тільки поверхом вище.

<details>
<summary>Оригінальний промпт (для історії)</summary>

```
[ШАПКА]

ЗАДАЧА: E10 — стадія підтвердження вимог і повної регресії агентами.
Опис: docs/plans/team_platform_architecture.md §9.

РОБИТИ ОСТАННІМ. Критерії входу (§9) прямо називають: усі E0–E8 змержені
за фіче-флагами, E9.2–E9.4 зелені, E9.8 без відкритих High/Critical,
розгорнутий стейджинг. Станом на 2026-08-06 E9 закритий, але E8 не
починався — тобто вхідні критерії ЩЕ НЕ виконані.

Правило вердикту з плану: PASS лише якщо перевірено ПОВЕДІНКУ, а не
наявність кнопки. Для кожної вимоги R1–R10 — трасований набір
«вимога → кейси → вердикт».
```

</details>

---

## Що варто зробити окремо від задач

**Відкрити PR.** Гілка `claude/e9-testing-integration-roles-e2e-83a1e8`
(13+ комітів, CI зелений) не має PR — з машини його створити неможливо.
База для стекового PR — `claude/testfortge-architecture-planning-9489f6`,
теж запушена. Опис лежить у репозиторії: `docs/plans/e9_delivery_record.md`.

**E0.6 — аудит env.** Виміряно: усі шість прапорців, оголошених у
engine/features.py (AUTH_ENABLED, ORG_MODE, EDITORS_ENABLED, DASHBOARD_V2,
WORKSPACE_DB_FIRST, STORAGE_BACKEND_CONFIGURABLE), присутні в render.yaml.
Тобто дрейфу в *прапорцях* немає. Лишається звірити секрети й неprапорцеві
змінні та ЗНАЧЕННЯ на живому сервісі — а це видно тільки з дашборда Render,
з коду не перевіряється.
