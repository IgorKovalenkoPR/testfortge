flask==3.1.0
flask-session==0.8.0
flask-wtf==1.2.1
flask-compress==1.15
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10
python-dotenv==1.0.1
python-docx==1.1.2
openpyxl==3.1.5
pypdf==5.1.0
Pillow==11.1.0
# pdf2image rasterises PDF mockups into per-page images for the
# /estimation Mockups vision pipeline (engine/mockup_vision.py).
# Needs the poppler-utils system package — see Dockerfile.
pdf2image==1.17.0
markdown==3.7
playwright==1.49.1
beautifulsoup4==4.12.3
requests==2.32.3
anthropic==0.97.0
# tenacity drives the retry/backoff policy in engine/llm_client.py
# (3 attempts, exponential 1s→4s) around Anthropic API transient errors.
tenacity>=9.0.0
# engine/qa_knowledge_loader loads YAML checklist + test-case bundles
# at boot and validates them against the schemas in engine/qa_knowledge/schema.
PyYAML>=6.0
jsonschema>=4.0
# Stage 3 (engine/live_executor): per-process RSS sampling for OomGuard.
# Render free tier OOM-kills the worker around ~480 MB resident, so the
# guard preempts at MEMORY_BUDGET_MB (default 400) by writing a partial
# result and exiting gracefully. live_executor falls back to a no-op
# guard if psutil is absent, so the dependency is functionally optional.
psutil>=5.9.0
# Fernet, for encrypting each organisation's own Anthropic API key at
# rest (engine/llm_keys.py — E0.9). Present transitively today via other
# packages, pinned explicitly here because relying on a transitive
# dependency is how a fresh CI install breaks on a version bump nobody
# made.
cryptography>=42.0.0
# Argon2id password hashing (engine/auth.py — E1.2). The library's own
# defaults track RFC 9106; we do not pin cost parameters here, because a
# hand-picked memory budget ages badly and check_needs_rehash() upgrades
# stored hashes on the next login when the defaults move.
argon2-cffi>=23.1.0
